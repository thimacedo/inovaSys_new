name: InovaSys CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check Environment Variables (Mock for CI)
        run: |
          echo "VITE_SUPABASE_URL=http://localhost" >> .env
          echo "VITE_SUPABASE_ANON_KEY=mock-key" >> .env

      - name: Lint Check
        run: npm run lint

      - name: Type Check
        run: npm run build # O comando build já executa tsc

      - name: Run Unit Tests
        run: npm run test:coverage
